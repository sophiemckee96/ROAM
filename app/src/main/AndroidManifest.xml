<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.roam">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RateEdinburghActivity"></activity>
        <activity android:name=".RateBudapestActivity" />
        <activity android:name=".BudapestActivity" />
        <activity android:name=".EdinburghActivity" />
        <activity android:name=".LauterbrunnenActivity" />
        <activity android:name=".KrakowActivity" />
        <activity android:name=".TallinnActivity" />
        <activity android:name=".LisbonActivity" />
        <activity android:name=".budgetHomescreen" />
        <activity android:name=".HomescreenActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>